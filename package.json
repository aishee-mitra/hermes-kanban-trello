{
  "name": "hermes-kanban-trello",
  "version": "0.1.0",
  "private": true,
  "description": "Trello-style 5-column Kanban dashboard skin for Hermes Agent (frontend-only over the built-in kanban backend).",
  "license": "MIT",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.5.0",
    "@types/react": "^18.3.0"
  }
}
